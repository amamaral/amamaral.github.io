\documentclass[crop, convert={outext=.svg,command=\unexpanded{pdf2svg \infile\space\outfile}}]{standalone}
\usepackage{tikz}
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{10pt}

\usepackage{siunitx}
\usepackage[american]{circuitikz}
\usepackage{amsmath}
\begin{document}
\begin{tikzpicture}
\draw
    (0,0) to[short] ++(0,2) to[R,l=$R_2$] ++(2,0) to++(0,-2) to[R,a=$R_1$] ++(-2,0) to[open]++(1,-0.2) node[label=below:(a)]{}

    (4,0) to[V, l=$v_n$, i^>=$i_n$, invert] ++(0,2) to[R,l=$R_2$] ++(2,0) to++(0,-2) to[R,a=$R_1$] ++(-2,0) to[open]++(1,-0.2) node[label=below:(b)]{}

    (2,-3) to[V, l=$v_n$, i^>=$i_n$, invert] ++(0,2) to[short] ++(2,0) to[R,l=$R/2$]++(0,-2) to[short] ++(-2,0) to[open]++(1,-0.2) node[label=below:(c)]{}
;
\end{tikzpicture}
\end{document}
